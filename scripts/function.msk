
module main

import log
import os

// Accessible from other file
export func hello() {
   log.print("hello")
}

func there(x string) {
   log.print("there")
   log.print(x)
}

func main() {
   hello();

   there(os.args[0])
}
